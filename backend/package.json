{
  "name": "backend",
  "version": "1.0.0",
  "type": "module",
  "packageManager": "bun",
  "scripts": {
    "build": "npm run build:stamp && cd ../frontend && npm ci && npx vite build --outDir=../backend/frontend/dist --emptyOutDir",
    "build:stamp": "bash ./scripts/print-build-stamp.sh",
    "build:web": "tsc -p ../web/tsconfig.json",
    "start:web": "node ../web/dist/server.js",
    "migrate": "tsx db/runMigrations.ts"
  },
  "dependencies": {
    "@clerk/backend": "^1.27.0",
    "encore.dev": "^1.53.4",
    "leap-app": "file:..",
    "pg": "^8.12.0"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "tsx": "^4.19.2"
  }
}
