{
  "name": "backend",
  "version": "1.0.0",
  "type": "module",
  "packageManager": "bun",
  "scripts": {
    "build": "cd ../frontend && bun install && npx vite build --outDir=../backend/frontend/dist",
    "build:web": "tsc -p ../web/tsconfig.json",
    "start:web": "node ../web/dist/server.js"
  },
  "dependencies": {
    "@clerk/backend": "^1.27.0",
    "encore.dev": "^1.53.4",
    "leap-app": "file:..",
    "pg": "^8.12.0"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
